enum Role { student, staff, other, unknown }

//Role fromString(String roleString) {
//  if (roleString == null || roleString.isEmpty) {
//    return Role.unknown;
//  }
//  return Role.values.firstWhere((r) => r.toString() == 'Role.' + roleString);
//}
